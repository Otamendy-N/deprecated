//este comando tiene problemas 
/*
exports.run = (client, message, args, Discord) => {
    const sumAll = (...args) => {
        args.forEach(element => {
            parseInt(element)
        });
        let result = 0;
        for (let i = 0; i < args.length; i++) {
            result += args[i];
            return result;
        };
    };

    message.channel.send(`esto es lo que sale: \n${sumAll(args)}`);
}*/